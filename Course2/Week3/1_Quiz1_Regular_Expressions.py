#--------------------------------------------------Practice Quiz: Regular Expressions--------------------------------------------------


#--------------------------------------------------
# 1.
# When using regular expressions, which of the following expressions
#  uses a reserved character that can represent any single character?

# re.findall(^un, text)

# re.findall(fu$, text)

# re.findall(f.n, text)
    # Correcto
    # Nailed it! The dot (.) represents any single character.

# re.findall(f*n, text)



#--------------------------------------------------
# 2.
# Which of the following is NOT a function of the Python regex module?

# re.grep()
    # Correcto
    # Keep it up! The grep command utilizes regular expressions on Linux,
    # but is not a part of the standard re Python module.

# re.search()

# re.findall()

# re.match()



#--------------------------------------------------
# 3.
# The circumflex [^] and the dollar sign [$] are anchor characters. What do these anchor characters do in regex?

# Match the start and end of a word.

# Match the start and end of a line
    # Correcto
    # Nailed it! the circumflex and the dollar sign specifically match the start and end of a line.

# Exclude everything between two anchor characters

# Represent any number and any letter character, respectively



#--------------------------------------------------
# 4.
# When using regex, some characters represent particular types of characters.
#  Some examples are the dollar sign, the circumflex, and the dot wildcard. What are these characters collectively known as?

# Special characters
    # Correcto
    # Awesome! Special characters, sometimes called meta characters, 
    # give special meaning to the regular expression search syntax.

# Anchor characters

# Literal characters

# Wildcard characters



#--------------------------------------------------
# 5.
# What is grep?

# An operating system

# A command for parsing strings in Python

# A command-line regex tool
    # Correcto
    # Right on! The grep command is used to scan files for a string 
    # of characters occurring that fits a specified sequence.

# A type of special character



